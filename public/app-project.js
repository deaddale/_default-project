var app = new Vue({
    el: '#app',
    data: {
		// isActiveHeader: false
    },
	computed: {
	},
	methods: {
		// showHeader: function() {
		// 	this.isActiveHeader = !this.isActiveHeader;
		// }
	}
});